name: Build and Push Spex Images

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-arm64:
    name: Build ARM64
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - dockerfile: ./docker/app.Dockerfile
            image: ghcr.io/spex-labs-co/workflow-builder-simstudio
          - dockerfile: ./docker/app.Dockerfile
            image: ghcr.io/spex-labs-co/workflow-builder-sim-worker
          - dockerfile: ./docker/db.Dockerfile
            image: ghcr.io/spex-labs-co/workflow-builder-migrations
          - dockerfile: ./docker/realtime.Dockerfile
            image: ghcr.io/spex-labs-co/workflow-builder-realtime

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate ARM64 tags
        id: meta
        run: |
          IMAGE="${{ matrix.image }}"
          echo "tags=${IMAGE}:latest-arm64,${IMAGE}:${{ github.sha }}-arm64" >> $GITHUB_OUTPUT

      - name: Build and push ARM64 image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          provenance: false
          sbom: false

  create-manifests:
    name: Create Multi-Arch Manifests
    runs-on: ubuntu-latest
    needs: [build-arm64]
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: ghcr.io/spex-labs-co/workflow-builder-simstudio
          - image: ghcr.io/spex-labs-co/workflow-builder-sim-worker
          - image: ghcr.io/spex-labs-co/workflow-builder-migrations
          - image: ghcr.io/spex-labs-co/workflow-builder-realtime

    steps:
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push manifests
        run: |
          IMAGE_BASE="${{ matrix.image }}"

          docker buildx imagetools create \
            -t "${IMAGE_BASE}:latest" \
            "${IMAGE_BASE}:latest-arm64"

          docker buildx imagetools create \
            -t "${IMAGE_BASE}:${{ github.sha }}" \
            "${IMAGE_BASE}:${{ github.sha }}-arm64"
